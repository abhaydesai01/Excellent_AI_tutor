[build]
  command = "npx prisma generate && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Proxy ALL API routes to EC2 backend (full Next.js app running there)
[[redirects]]
  from = "/api/*"
  to = "http://3.108.61.212/api/:splat"
  status = 200
  force = true
